# Optionale Zusatzmodule / optional extras
# Die Bild-Toolbox laeuft auch ohne diese Pakete - einzelne Funktionen sind
# dann nicht verfuegbar. Der Reiter "Info & Hilfe" zeigt an, was installiert ist.

send2trash>=1.8      # Loeschen in den Papierkorb statt endgueltig
icoextract>=0.1.4    # Icons aus EXE- und DLL-Dateien lesen
pillow-heif>=0.13    # HEIC- und HEIF-Dateien oeffnen (AVIF bringt Pillow ab 11.3 selbst mit)
